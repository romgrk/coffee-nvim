// Generated by CoffeeScript 1.10.0
(function() {
  var PORT, clients, complete, history, log, logger, net, rd, readline, server, sk,
    slice = [].slice;

  net = require('net');

  readline = require('readline');

  logger = require('romgrk-logger');

  log = logger(console.log);

  PORT = 5000;

  sk = null;

  clients = [];

  log.method = function() {
    var args;
    args = 1 <= arguments.length ? slice.call(arguments, 0) : [];
    return process.stdout.write(args.join(' '));
  };

  server = net.createServer(function(socket) {
    sk = socket;
    clients += socket;
    log.a('Connected: ' + sk.remoteAddress + ': ' + sk.remotePort, '\n');
    socket.on('data', function(d) {
      return log.text(log.erase.line + d.toString());
    });
    return socket.on('end', function() {
      log.a('Disconnected\n');
      return sk = null;
    });
  });

  server.listen(PORT);

  log.a("Listening on port " + PORT + "...\n");

  history = [];

  complete = function(line) {
    return [history.slice(0, 1), line];
  };

  rd = readline.createInterface({
    input: process.stdin,
    output: process.stdout,
    completer: complete
  });

  rd.on('SIGINT', function() {
    return process.exit(0);
  });

  rd.on('line', function(line) {
    history.unshift(line);
    if (sk != null) {
      return sk.write(line);
    } else {
      return log.a('No socket\n');
    }
  });

  rd.on('close', function() {
    log.warn('Exiting.');
    return process.exit(0);
  });

}).call(this);
