// Generated by CoffeeScript 1.10.0
(function() {
  var Clc, Net, Path, attach, await, defer, fiber, main, sync;

  Net = require('net');

  Path = require('path');

  Clc = require('cli-color');

  attach = require('neovim-client');

  sync = require('synchronize');

  fiber = sync.fiber;

  await = sync.await;

  defer = sync.defer;

  main = function(nvim) {
    return nvim.command("EchoHL TextInfo " + ("'RPC: " + (Path.basename(__filename)) + " " + process.pid + "'"));
  };

  attach(process.stdout, process.stdin, function(err, nvim) {
    nvim.on('request', function(method, args, resp) {
      var msg;
      msg = method + ' ' + args;
      return nvim.command("EchoHL TextError \'Request: " + msg + "\'");
    });
    nvim.on('notification', function(method, args) {
      var msg;
      msg = method + ' ' + args;
      return nvim.command("EchoHL TextWarning \'Notification: " + msg + "\'");
    });
    return main(nvim);
  });

}).call(this);
